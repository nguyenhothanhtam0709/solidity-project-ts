import { task, types } from 'hardhat/config';

task('deploy', 'Deploy contract')
	// .addParam(
	// 	'contracts',
	// 	'list of contracts to deploy',
	// 	'all',
	// 	types.string,
	// 	true
	// )
	.setAction(async (taskArgs) => {
		// const accountNames = console.log(taskArgs.contracts);
	});
